# WEFT / PPL — V1 实施与验证记录

### 场景与模型标签双向联动 · 2026-09-17

修复制作图示仅接收 mode、未接收 focusedNode 的断点。ProductionScene 与底部 inspector 共用 reducer；增加 pinnedNode / SELECT_NODE / CLEAR_NODE，支持上下悬停、键盘焦点、点击 / 触摸固定、再次点击 / 空白 / Escape 取消。语义热点覆盖模块、身份、版本、依赖、校验、解析、状态、工作空间与五条未来分支；直接关联高亮，邻接关系保留，其他部分减弱。独立交互 wrapper 控制强调，不修改 GSAP 的形态时间线；隐藏分支不可聚焦。手机纵向布局拉开资产与未来分支间距，避免热点重叠。静态构建与 8 项语言 / 场景 / 双向联动定向测试通过，含触摸、键盘、固定回退及 02 不重播、03 中断恢复。示意图不作为生产证据，未部署。

### 整页中 / En 切换 · 2026-09-17

位置修正：用户随后确认放在导航右侧，现已移至状态区域之后；首页与占位章节一致。构建及三宽度语言切换 / 按钮位置检查通过。

导航最左侧新增键盘可用的语言按钮，默认保留中文中英混排，英文词典覆盖首页正文、场景标签、模型说明、无障碍名称和临时编辑面板；占位章节也可切换。页面保留 Server Component，使用轻量客户端语言上下文；不重置画布模式，不保存语言偏好，刷新恢复中文。临时编辑按语言隔离，中文继续输出原 schema 1，英文输出带 language 与翻译源文件的 schema 2。静态构建与三宽度语言 / 场景检查通过；实际生产证据、部署状态未改变。

### 02 白色原型连续变形 · 2026-09-17

按用户要求，右侧在进入 02 时先显示白色 v1 原型约 0.85 秒，然后用 1.35 秒把同拓扑点阵与边界轮廓连续插值到新版斜向拱起曲面，颜色同步变为薄荷绿；版本示意统一为 v1 → v2。旧版预览仍为原始形态，新版源资产与右侧最终形态一致。快速切换时清理时间线并设置目标几何；编辑或 reduced motion 直接显示当前最终形态。构建通过，原型 / 中间态 / 最终态 / 中断、三宽度切换和 03 过渡共 3 项定向检查通过。未重测完整性能指标或全站回归，未部署。

### 03 视角分支展开过渡 · 2026-09-17

切换至 Extend 时，五条未来关系通过 SVG mask 从系统端逐条显露，保持虚线语义；分支间隔 0.12 秒、线条展开 0.8 秒，端点与文字随后缩放 / 上浮淡入，整段约 1.6 秒。沿用组件 GSAP context，中断切换和卸载时恢复初态；编辑和 reduced motion 直接显示完整静态内容。静态构建通过，三宽度切换与专门的中间态、错峰顺序、中断、重入、reduced-motion 清理检查 2 / 2 通过；已查看 artifacts/production-scene-extend.png。未重跑全站回归，未部署。

### 制作场景后续修正 · 2026-09-17

按用户最新反馈，将人形替换为参考图方向的双峰起伏、密集点阵曲面；可见标签改为“资产”，更新说明为“资产版本替换”。源模块、旧版本预览和工作空间复用同一曲面。曲面使用确定性采样和轮廓 SVG 绘制，不作为真实生产截图。此决定覆盖下文先前的角色方案。静态构建与三宽度场景回归通过，查看桌面截图后增强点阵和轮廓线宽；未重跑全站测试，未部署。

## Master Canvas 制作场景版 · 2026-09-17

用户确认以 mid_1 / mid_2 / mid_3 为示意方向，落实独立成果 → 系统组织 → 协同工作空间的构图。相机、场景和中性角色通过同一轴测投影生成，左右复用几何；角色替代“布料”，四边取景面替代扇形视锥。图形明确为结构示意，版本号仅作说明，不是实际生产资产或 UI。

新增 production-geometry.ts、ProductionScene.tsx / CSS；固定点阵合并为 SVG path 并通过 defs / use 复用，本版无需 Canvas 持续绘制或 PNG 资产。Organize 显示身份 / 版本 / 依赖 / 校验，Transform 显示旧版与更新路径、保留相机和场景，Extend 显示五条未来虚线。GSAP 仅在模式或 reduced-motion / 编辑状态变化时运行有限时间线，清理与快速中断保持有效。

MasterCanvas 保留原 reducer、共享语义模型、检查说明、键盘、触控和 EXPLORE 路由；模型检查按钮移到图下方，三个视角控件横排，手机场景纵向排列。桌面在导航与关闭的模型说明一起占用一屏；展开说明继续正常流。此前抽象节点坐标和 ambient 测试已调整到新的按钮与有限动画行为。

验证：静态构建通过；场景、首页、四种桌面一屏尺寸、说明展开、临时编辑共 23 项回归通过；随后截图隔离样式校正，场景检查再次通过。截图 artifacts/production-scene-1440.png、768.png、390.png。已检查实际桌面与手机截图；自动无障碍、快速切换、键盘、触屏、reduced motion 和无 JS 正文覆盖，不代表最终用户视觉验收。未重测完整 Hero / Summary / Footer 动画或性能指标，未在实体手机测试，未提交、推送或部署。已有 Summary 的 Diagrams.tsx 改动保留。

日期：2026-09-16。当前为可评审实现，未发布。依据当前 AGENTS.md、技术 V2.2 与本次用户的导航 / 细胞图形修正；历史规划和用户已有改动未被覆盖。

## 已实现

- Next.js 16.3.5 App Router、React、TypeScript、CSS Modules，构建时 Server Components，静态导出到 `out/`。本地内容与图模型、状态、视觉目标、动效、渲染分离。
- Header → Hero → System Summary → Master Canvas → Production Proof → Footer。暖白 / 炭黑、薄荷绿、细线、裁切标记与目标图构图。
- Hero 只作 Boot / Resolve；Summary 首次进入播放三阶段序列并保持终态。SVG 纹理按最新反馈改为细胞组织：扰动和轻微扭转的分布，由相邻位置共同生成 Voronoi 边界，内缩并圆滑转角。保留中心较大、外围渐小的整体趋势，限制疏散区域的最大尺寸；相邻细胞存在自然差异，不再严格逐环递减。固定种子和缓存保持 SSR、hydration 与视角切换稳定，没有逐帧计算细胞几何。
- 共享八个核心节点与固定依赖图的 Overview / Organize / Transform Preview / Extend。reducer 保证合法状态；GSAP 从当前视觉状态中断并趋向最新目标。未来分支只在 Extend 中以虚线出现。
- 桌面 hover / focus、点击锁定、键盘方向键 / Home / End / Escape、移动触控、节点检查、文字模型、reduced motion、离屏 / 隐藏文档暂停 Canvas；开发环境 D 键显示状态调试信息。
- 按最新导航决定：页眉、独立 EXPLORE 与 VIEW EVIDENCE 真正切换路由。`/systematic-thinking/`、`/design-innovation/`、`/next/`、`/evidence/` 是已静态导出的最小“内容尚未开放”目标页，含返回首页链接；没有冒充完整章节正文。
- 临时试编辑：23 个纯文本白名单字段，正式内容和注册表在 `src/content/home.ts`；内存覆盖、字段 / 全部恢复、关闭重开保留、刷新还原、真实差异 JSON、复制与失败回退。Provider 与文本叶组件不把首页改成整体 Client Component；编辑状态独立于画布。
- 生产截图明确标记待补，保留两人贡献归属与项目资料来源说明。邮箱没有提供，因此不显示 Contact。

## 内容与图形依据

| 区域             | 依据与读者结论                           | 图形 / 证据状态                      | 深层目标                |
| ---------------- | ---------------------------------------- | ------------------------------------ | ----------------------- |
| Hero             | Homepage Summary：跨软件成果交接与组合   | 原生 SVG 抽象 Resolve，不冒充生产 UI | 无                      |
| Summary          | Homepage Summary：独立变化，共享生产模型 | 三阶段概念图，非运行时录屏           | 无                      |
| Master Canvas    | V2.2 保留的 V2.1 图、状态与交互契约      | 固定共享模型；实线现有 / 虚线未来    | 三个最小未开放目标页    |
| Production Proof | AGENTS 与内容计划记录的公开事实          | 两张真实脱敏截图缺失；仅框架占位     | 最小 Evidence 未开放页  |
| Footer           | final.png 关系图与布局                   | People / Data / Software 概念图      | Evidence / 明确返回顶部 |

## Hero 动画与检查更新 · 2026-09-17

**当前修订：** 新簇与相邻簇增加 10 条灰色曲线（灰色连接共 99 条）。左侧标题、副标题、说明、底部事实均支持半径 84px 内逐字距离衰减放大，中心最大 1.22 倍，保持原换行和布局。完整原文保留在无障碍文本层，逐字视觉层对辅助技术隐藏，避免标题被读作分隔的单字。编辑时恢复普通纯文本。

底部三项事实使用独立 `HeroFactsMotion`：首次进入逐字显示，550ms 起始停顿，65–170ms 字间顿挫，1.15 秒项间停顿，最后保留光标 900ms 后完成。闪烁光标与字形位置同步；预留全文空间，不推挤后续文字。离屏和隐藏文档暂停计时，卸载清理；编辑 / reduced motion 显示全文，不重复播放。该组件仅控制可见性与光标，逐字放大独立控制 transform，未修改事实内容和白名单。

验证：静态导出与类型检查通过。完整 24 项回归中 23 项通过，逐字恢复检查把空 style 属性误认为残留变换；将断言改为检查实际 transform 后，该项单独复测通过。涵盖三视口（390 / 768 / 1440）、axe、编辑隔离 / 复制 / 纯文本、导航、快速切换、Hero 生命周期、字间及项间停顿、打字离屏暂停、字距放大与复原、实时 reduced motion。实际静态服务器上的截图为 `artifacts/hero-typewriter-pause.png` 和 `artifacts/hero-text-lens.png`，录像为 `artifacts/hero-typewriter.webm`。

本机 Edge 153 / Windows 11 / i9-14900HX，1440×1000、无网络或 CPU 限速：Hero 119 帧采样约 60.01fps，p95 16.7ms；全页实验室 LCP 164ms、CLS 0.0112、脚本交互 INP 56ms。以上不是用户现场数据，低性能设备、实体触屏、Safari / Firefox 和人工阅读测试仍未验证。真实生产截图待补，未部署。

**前次紧凑簇修订（标题整体缩放已由下述逐字效果取代）：** 删除标题下方的大背景簇，在用户红圈附近（标题右侧，SVG 中心 65/165）放置紧凑细胞簇。仍使用完整 Hero 画布作为随机散落入场范围。标题悬停缩放 1.035，左侧为缩放原点，不改变副标题与正文布局；调整标题命中层级，编辑态及 reduced motion 下停用缩放。构建（含类型检查）及 **12 / 12 定向检查通过**，涵盖三视口普通/编辑态、标题放大与恢复、无布局位移、新簇位置、原有 Hero 生命周期及悬停。此次未重跑全部导航与编辑回归。Hero 本机采样 60.01fps / p95 16.8ms，低性能设备未验证。`home-1440.png`、`hero-curves.png`、`hero-intro.webm` 已更新。

前次追加记录：悬停簇附近时，在原有局部细胞放大之外，让细胞相对簇中心径向外移（普通簇最大额外约 48%，当时的左侧背景簇约 16%）；中心绿节点随接近程度提高饱和度，离开后平滑恢复。桌面画布向左扩展至标题下方，当时的左侧背景簇扩大、细胞缩为原尺寸的 58%、降低透明度。移动端保留右侧主图尺度与正文分区。修改涉及 `hero-boot.ts`、`Diagrams.tsx`、`page.module.css` 和 `hero.spec.ts`。

该追加版本静态构建（含类型检查）与 **11 / 11 定向检查通过**：三视口普通态/编辑态、axe、入场/呼吸、簇展开、绿节点变化及恢复、reduced motion 与暂停清理。实际截图 `artifacts/hero-lens.png` 已包含标题下方底纹和展开高亮状态；`home-1440.png` 为全页静态布局。Hero 同环境复测 60.01fps、p95 16.8ms（119 帧）；未在本次重跑全部导航/编辑回归或低性能设备检查。

- 用户最新确认覆盖此前 Hero 仅淡入的行为：随机散落 → 先慢后快聚簇（power2.in）→ 原有连线与 Resolve；约 3.55 秒完成入场。Summary 保留独立三阶段动画。
- `src/components/hero-boot.ts` 从原始 SVG 获取每个细胞轮廓。GSAP 只控制入场进度，一个 RAF 负责绘制；轮廓预先绘入 3× 图集，避免每帧重新处理数千条矢量路径。
- 最新呼吸增强：八个簇分别使用固定随机种子的相位、3.8–6.1 秒主周期与不同振幅；叠加较慢波形，形成同时刻不同方向、不同程度的连续起伏。细胞尺寸振幅参数为 18–30%，簇径向舒展最多约 8.5%，外围上下位移参数为 3–7 SVG 单位，中心附近衰减以保持绿色节点对齐；这些是参数上限，实际瞬时幅度由波形决定。鼠标影响区半径约 84 CSS px，中心放大最多约 2.2 倍，离开平滑恢复。
- 灰色底层连接由 51 条增至 89 条（含 12 条新增下方弧线），六条绿色连接也全部改为三次 Bézier 曲线，连接端点和语义关系不变。修改仅针对 Hero。
- 离屏、隐藏文档时暂停；进入编辑或 reduced motion 时清理 Canvas、事件和观察器，恢复原始 SVG。退出编辑恢复持续效果，不重播入场。触屏没有鼠标悬停放大，但保留入场、呼吸及正常滚动。
- 前次完整回归 **22 / 22 通过**。此次独立呼吸与曲线调整后，静态构建（含类型检查）及 **8 / 8 定向复测通过**：390 / 768 / 1440px、axe、入场、曲线、各簇不同步、鼠标放大恢复、编辑/reduced-motion 清理及离屏暂停。此次未重跑全部编辑与导航测试。隐藏文档仍采用模拟事件，不代表真实 OS 切换验证。
- 持续矢量绘制首次测得约 29.6fps；图集缓存后 Hero 的 119 帧采样为 **60.01fps、p95 16.8ms**。Windows 11、i9-14900HX、Edge 153、1440×1000、本地静态服务器、无 CPU/网络限速、录像开启。仅单次实验室结果，低性能设备未验证。最新网页采样 LCP 124ms、CLS 0.00784、INP 48ms。
- 最新记录：`artifacts/hero-intro.webm`（含成簇后的独立呼吸）、`hero-curves.png`、`hero-intro-scatter.png`、`hero-intro-clusters.png`、`hero-lens.png`、`hero-performance.json`。此次增强后重新测得 Hero **60.01fps、p95 16.8ms**，环境同上；网页 LCP/CLS/INP 为前次记录。动画与鼠标手感最终仍待用户确认。

## 前次细胞形状验证记录

- `npm run build`：通过；首页和四个最小目标页静态导出。`npm run typecheck`：通过。
- 此前完整功能回归 **17 / 17 通过**。本次细胞组织调整后，普通三视口、可访问性、12 次快速切换、键盘、resize 与性能的 **5 / 5 复测通过**，运行于实际 `out/` 静态服务器；本次未重跑编辑功能完整回归。
- 390 / 768 / 1440px：无横向溢出，自动 axe WCAG A/AA 检查无报告违规；普通态与编辑态均留存截图。自动检查不等价于完整人工辅助技术审核。
- 12 次连续快速视角切换，最后节点坐标与目标布局一致；离开 Extend 后未来边不可见；检查节点、键盘、触控、resize 后 HTML 命中区域与 SVG 节点中心对齐。
- reduced motion 动态切换、禁用 JS 仍可阅读正式内容、Canvas 离屏暂停 / 恢复。隐藏文档分支使用受控 visibilitychange 模拟，不声称已测试真实 OS 标签页切换。
- 编辑即时生效、关闭 / 重开、单项 / 全部恢复、刷新恢复、同一浏览器上下文中的两个独立页面隔离。修改不重置画布视角；编辑中 Canvas 暂停。
- 空字符串、中文、引号、换行、类似 HTML / script 的内容保持为纯文本；差异清单字段 ID、标签、原文、新文、schemaVersion 与 source 正确；恢复到原文的字段从差异中消失。
- 浏览器真实 Clipboard API 复制验证通过；拒绝剪贴板时出现可选择的只读文本，不显示虚假成功。输入法仅测试合成 composition 事件和中文文本输入，未人工测试 Windows / 手机真实 IME。
- 测试观察期间没有内容写入请求、localStorage / sessionStorage / cookie 写入；额外阻断这些写入及 IndexedDB 打开仍可操作。源代码未引入持久存储、内容 fetch、写接口或 HTML 注入渲染。
- 根路径与本地模拟 `/WEFT_PPL_Presentation` 子路径构建均通过资源加载、目标页刷新、返回首页与交互验证。子路径检查在细胞轮廓微调前执行；后续只改 SVG 图形，路由代码未变化。当前 `out/` 已恢复根路径版本。实际 GitHub 站点仍未部署，真实 URL 和托管配置未确认。

## 性能记录

最后测量：2026-09-16 16:15 UTC（北京时间 09-17 00:15）。Windows 11 build 26200、Intel i9-14900HX、Edge 153.0.4234.32、1440 × 1000、全新浏览器上下文、本地 loopback 静态服务器，无 CPU / 网络限速，正常动画。

| 指标                          | 本次实验室结果 |      目标 |
| ----------------------------- | -------------: | --------: |
| LCP                           |          112ms |  < 2500ms |
| CLS                           |        0.00784 |     < 0.1 |
| INP（脚本触发的真实指针交互） |           24ms |   < 200ms |
| 动画采样平均帧率              |       60.00fps |  约 60fps |
| p95 帧间隔                    |         16.9ms | 约 16.7ms |

帧采样 119 帧，超过 33.4ms 的帧为 0。这些是单次实验室样本，不是线上 RUM、低性能设备或真实用户体验保证。真实截图接入后需要重新测量。

## 视觉比对与剩余差异

已在浏览器截图中对照 final.png 检查六区顺序、比例、字体层级、留白、暖白 / 炭黑配色、三个控制器位置、线框模块与页脚。

- 使用自绘的共享动态图，不是目标图整图背景。细胞簇分布、线框透视、线条密度与参考仍不是逐像素一致。
- 英文使用本地打包的 Barlow Condensed，中文使用系统字体；参考图的具体字形与字重不能完全等同，跨平台字形仍可能变化。
- Overview 隐藏未来语义分支；切到 Extend 才显示五条未来方向，避免将未来能力当作已实现。
- 额外的节点说明、文字模型、团队归属与待补素材提示使页面略长。试编辑入口是 V2.2 新增；面板仅按需出现。
- 移动端把浮动控制器改为顺序排列，保持可操作性；没有按桌面图机械缩小。

最终视觉批准仍由用户对照目标图完成。本记录是实现者自查，未替代人工视觉签收或 30–60 秒读者理解测试。

## 交付截图与记录

- `artifacts/home-1440.png`、`home-768.png`、`home-390.png`：普通浏览态。
- `artifacts/hero-tissue.png`：最新细胞组织图形，实际静态网页 2× DPR 局部截图；hero-cells.png 保留为上一版环状排列的历史截图。
- `artifacts/view-organize.png`、`view-transform.png`、`view-extend.png`：三个视角。
- `artifacts/editor-1440.png`、`editor-768.png`、`editor-390.png`：编辑态。
- `artifacts/evidence-unavailable.png`：Evidence 最小目标页。
- `artifacts/performance.json`、`export-root.json`、`export-subpath.json`：测量和静态路径检查记录。

## 尚未验证 / 待提供

真实生产截图及脱敏审核、生产系统运行时的独立核验、联系邮箱、人工视觉批准、真实读者测试、真实输入法 / 手机、Safari / Firefox、线上网络与真实用户性能、真实 GitHub Pages URL / 配置 / 发布。尚未提交或推送 Git；本次没有部署，也未声称正式内容验收通过。

## 模型文字说明展开重叠修复 · 2026-09-17

已在 768px / 1440px 静态页面复现：details 展开增大 Master Canvas 总高度，三个绝对定位的视角入口和底部当前视角说明随百分比坐标移动，进入展开的文字区。390px 原本使用顺序布局，未复现同一位移。

修改 `MasterCanvas.tsx` / `MasterCanvas.module.css`：用独立 composition 容器定位图形、视角入口与当前视角说明，details 作为其后的正常流内容；底部操作栏通过纵向弹性布局留在说明下方，修正 768px 的局部贴靠。图结构、视角状态和跳转不变。

新增 `tests/model-disclosure.spec.ts`，覆盖 390 / 768 / 1440px 展开与收起、键盘 Enter / Space、四种视角、控件坐标不随 disclosure 改变、说明与控件无相交、无横向滚动。静态构建及 11 项定向回归通过（含首页三视口 / axe、12 次快速切换、触控、reduced motion、Canvas 暂停和无 JavaScript 内容）。已查看 `artifacts/model-expanded-1440.png` / `768.png` / `390.png`；组件截图隐藏页面固定导航，避免长截图拼接遮挡。本次没有重跑全部编辑、Hero 动画或性能采样，未部署，实体设备未验证。

## 导航收起与状态轮播 · 2026-09-17

新增 `AutoHeader.tsx` / `AutoHeader.module.css`，首页仍通过 Server Component 传入导航内容。桌面导航高度由 112px 降至 78px，采用不透明暖白背景。Hero 只要仍在视口内就常驻展开；离开 Hero 后闲置约 800ms 向上收起，留下 8px 白边（小屏 10px）。鼠标靠近顶部展开、离开收起；触屏白边提供 44px 点击范围；Tab 进入链接展开，Escape 收起，焦点不会被困住。编辑和 reduced motion 保持展开。

右侧三条原有信息逐条从下向上进入、上一条向上离开，9 秒一轮，单条约 2.5 秒静止。点击可暂停 / 继续，悬停和键盘焦点时暂停；收起或文档隐藏时暂停动画；编辑、reduced motion、无 JavaScript 时显示完整静态信息。未更改事实或路由。

复测中定位到 document 的 110px 锚点预留空间会让 sticky 导航的焦点触发额外滚动；为导航控件抵消该偏移，并用 preventScroll 转移 Escape 焦点。测试采用可见屏幕触点及原生 Tab，验证全过程保持阅读位置。

最终静态构建与 23 项定向检查通过：390 / 768 / 1440px 的 Hero 常驻、离开收起、鼠标 / 触屏 / 键盘展开、返回 Hero 自动展开、状态轮播和暂停，以及已有编辑、导航、首页和模型说明回归。已查看 `artifacts/header-hero-1440.png`、`header-hero-390.png`、`header-expanded-1440.png`、`header-collapsed-1440.png`。本次未重跑 Hero 粒子性能、实体手机、Safari / Firefox 或真实用户测试，未部署。

## 生产信息打字效果 · 2026-09-17

Production Proof 的 HOSTS / SYSTEM / WORKFLOW / STATUS 四行复用 Hero 的打字执行逻辑，依次打出标签和内容。字间 28–75ms（Hero 为 65–170ms），标签到内容间停顿 180ms，行间停顿 600ms，初始停顿 320ms，完成后光标保留 650ms 再移除。保持 dt/dd 语义、原有文案、列宽和完整文本布局；装饰逐字层独立于完整无障碍原文，不引入事实字段编辑。

修改 `HeroFactsMotion.tsx` / CSS、`GlyphText.tsx` 和 `page.tsx`；新增 `tests/production-typing.spec.ts`。离屏和隐藏文档暂停，组件卸载清理；编辑和 reduced motion 显示全文且不重播，无 JavaScript 时保留全文。

静态构建通过，8 项定向检查通过：390 / 768 / 1440px 首次进入、逐字速度和行间停顿、光标、离屏暂停、完成后布局尺寸保持、原文一致、编辑与 reduced motion 清理，以及原 Hero 打字、三视口首页 / axe 回归。已查看三种宽度完成截图；录像 `artifacts/production-typewriter.webm`，截图 `artifacts/production-typing-complete-1440.png`。本次未重跑全部导航 / 画布测试或性能采样，实体设备未验证，未部署。

## Hero 绿色节点悬停 Bloom · 2026-09-17

按用户最新视觉要求，在已有绿色节点的簇被鼠标靠近时，为节点添加两层柔和绿色 drop-shadow（4px / 12px）。透明度跟随现有簇 proximity 的平滑值，保留饱和度变化；离开后渐弱至关闭，无绿节点的簇不产生光晕。沿用同一 RAF 更新，不新增持续循环；原有清理逻辑恢复 SVG 样式，reduced motion 下无发光。

修改 `src/components/hero-boot.ts` 与 `tests/hero.spec.ts`。静态构建及 5 项 Hero 定向检查通过：三视口入场、编辑 / reduced motion 清理、靠近发光和离开恢复、无绿节点不发光、离屏 / 隐藏暂停。已查看 `artifacts/hero-bloom.png`。本机 Edge 153，1440×1000、无 CPU / 网络限速、开启录像，悬停发光中采样 119 帧，平均约 60.00fps、p95 16.8ms；仅为实验室样本，实体移动设备与低性能设备未验证。本次未重跑全部站点回归，未部署。

## 移除装饰加号 · 2026-09-17

按用户要求，移除 Hero / Master Canvas / Footer 的加号裁切标记，以及生产截图占位框中的装饰加号；删除相应 CSS。保留模型说明展开箭头、导航功能和图形连接。修改 `page.tsx` / `page.module.css`、`MasterCanvas.tsx` / CSS。

源码搜索确认没有残留 cropMarks / crops / 装饰加号元素。静态构建及 6 项现有回归通过：390 / 768 / 1440px 首页布局、axe、模型说明展开收起与四种视角。截图 `artifacts/home-1440.png` 和 `artifacts/model-expanded-768.png` 已更新。未重跑全部动画 / 性能检查，未部署。

## System Summary 延迟与阶段停顿 · 2026-09-17

修改 `MotionFigure.tsx` 中 Summary 专属时序：图形至少一半进入视口后初始化，顶部进入视口高度 65% 的位置才触发时间线，避免仅在 Hero 下方露头时消耗动画。开始前等待 1 秒；保留原有 0.5 / 0.65 / 0.85 秒过渡时长，阶段之间各加入 1 秒停顿，总时间约 5 秒。首次播放后保持完成态，不改变 Hero 和 Footer 的触发 / 时序。

静态构建及 9 项定向检查通过。新增 `tests/summary-motion.spec.ts` 验证提前露出不播放、进入后的延迟、两次停顿、完成后不重播及 reduced motion 完整显示；其余检查覆盖三视口 / axe、画布快速切换、触控、离屏暂停和静态 HTML。实际录像 `artifacts/summary-delayed.webm`。本次未重跑全部 Hero / 编辑回归或性能测试，未部署。

## Git 代码发布检查点 · 2026-09-17

用户授权将当前项目提交并推送到现有仓库 Ace199/WEFT_PPL_Presentation 的 main。当前静态构建已通过，发布前完整浏览器回归 36 / 36 通过（本机 Edge 153，实际 out/ 静态服务器）。代码、项目文档及已有验证素材一并纳入检查点，保留当前工作区的文档修订与参考图迁移。构建目录、依赖和测试临时输出由 .gitignore 排除。

本次仅发布 Git 代码版本，没有新增 GitHub Pages 工作流或启用网站托管；不表示网站已上线。真实生产截图、邮箱、最终人工视觉确认及读者测试仍待补。

## System Summary 第二簇从空白显示 · 2026-09-17

按用户要求，将 `MotionFigure.tsx` 中第二阶段的起始 opacity 从 0.15 改为 0，保留既有入场延迟、阶段停顿和位移。静态 / reduced-motion 模式继续完整显示图形。

在修改前用现有 Summary 浏览器检查复现等待期间 opacity 为 0.15；更新断言后，修改前失败、修改后通过。静态构建通过，实际静态服务器上的 Summary 定向检查 1 / 1 通过，覆盖透明起点、延迟、停顿、完成态、不重播和 reduced motion。录像更新为 `artifacts/summary-delayed.webm`。本次未重跑完整回归，未提交或推送此次修改。

## 首屏填满、画布一屏布局与边界导航 · 2026-09-17

按用户后续澄清，Hero 的目标是“导航 + Hero 填满首屏、不露出下一块”，而非单纯上移。`page.module.css` 去掉原有固定高度，按视口减去实际导航高度计算；`AutoHeader.tsx` 用 ResizeObserver 同步可换行导航的高度。保持正常文档流。极矮窗口保留最小内容高度，允许滚动以免压缩或遮挡文字。

Master Canvas 原布局在 1440×800 时实测高 1136px。`MasterCanvas.module.css` 对宽度大于 1000px 的桌面按视口高度分配标题、图形和控件，预留导航及折叠说明空间；同视口修改后高 708px。图形与命中区域共用原坐标，四视角结构不变。手机保留纵向排版，展开说明继续在正常文档流中向下延伸。三条装饰斜线统一线宽、长度和 45° 方向，起点与文字块保留 24px 间隔，移动端继续隐藏。

导航以 Summary 顶线为边界：顶线接触导航底线后，按滚动距离逐渐上移，最多收至桌面 8px / 窄屏 10px 白边；向上滚动反向展开。手动悬停、触控和键盘展开仍保留。滚动事件只预约一次 RAF 写入 CSS 位移，React 仅在边界跨越时更新状态；卸载清理监听、观察器和待执行帧。编辑及 reduced motion 保持导航展开。

最终静态构建通过，实际 out/ 服务器上的定向回归 34 / 34 通过（Windows / Edge）。包括首屏 390×844、768×1000、1440×800、1920×1080 与高度变化；画布 1024×768、1366×768、1440×800、1920×1080 一屏、四模式与节点命中对齐；导航边界逐段收起、停住、反向、键盘/触控；说明展开、三宽度 axe、12 次快速切换、Hero 和 Summary 动画。修正 Summary 测试默认轮询退避导致的停顿计时误差，先等字体就绪，再密集观察阶段结束，动画参数未改。

已查看 `artifacts/hero-viewport-1440.png`、`master-viewport-1440.png`、`master-viewport-1024.png` 和 `header-boundary-half-1440.png`。本轮没有重跑全部编辑及生产打字检查、实体设备或完整 Web Vitals 测试；视觉仍待用户确认。未提交、推送或部署这些后续修改。

## Bloom 范围加倍 · 2026-09-17

按用户批注将 hero-boot.ts 两层绿色 Bloom 模糊半径从 4 / 12px 调整为 8 / 24px，保留颜色、不透明度与 proximity 渐变。静态构建及已有 Hero 悬停 / 移开恢复 / 无绿节点 / 离屏与隐藏暂停检查 1 / 1 通过；已查看更新后的 artifacts/hero-bloom.png。本次未重跑完整回归，未推送。

## 页脚三圆延迟与重播 · 2026-09-17

新增懒加载 `src/components/footer-motion.ts`，由 `MotionFigure.tsx` 独立挂载：图形至少一半进入视口后等待 1.5 秒，再播放原有两侧圆向中央靠拢的动画；第一次等待期间鼠标进入不跳过延迟，之后鼠标重新进入立即重播。复用单一时间线，连续重播先复位，不叠加动画。补充键盘 Enter / Space 与触控点击重播。离屏 / 文档隐藏时暂停；卸载、编辑及 reduced motion 清理并恢复静态三圆。Hero / Summary 参数不变。

静态构建及定向浏览器回归 12 / 12 通过。新增 `tests/footer-motion.spec.ts` 覆盖 390 / 1440px 初次 1.5 秒等待、10 次连续鼠标移入、最终归位、键盘、触控、离屏暂停、编辑与 reduced motion 清理；其余覆盖 Summary、三宽度 / axe、Master Canvas 快速切换与静态 HTML。已查看 `artifacts/footer-motion-1440.png`。本次未重跑完整 Hero / 编辑套件、完整性能指标或实体设备检查，未提交或推送。

## 刷新后导航首次滚动锁定修复 · 2026-09-17

复现路径：刷新 → 鼠标进入 Hero 导航 → 鼠标离开 → 首次滚到 Summary 边界。旧代码在 Hero 内提前退出 retire，留下 revealed=true，导致边界位移已计算却仍保持展开；实测应上移 30px 时仍为 0px。修改 `AutoHeader.tsx`：鼠标 / 焦点离开且仍在 Hero 时清除手动展开状态，Hero 常驻由 inHero 继续保证。

新增 `tests/header.spec.ts` 回归，修改前失败、修改后通过，并验证返回 Hero 后再次经过导航也正常。静态构建及导航 / 首屏定向检查 12 / 12 通过，覆盖 390 / 768 / 1440px 边界跟随、反向、手动展开、触控、键盘，以及四种首屏视口。未重跑完整站点回归或实体设备检查，未提交或推送。

## 导航状态持续轮播 · 2026-09-17

按用户要求移除 `AutoHeader.tsx` 中状态轮播暂停按钮、图标和暂停状态；改为带完整文本说明的非交互分组。同步删除 CSS 的悬停 / 聚焦暂停规则和相关按钮焦点分支。正常浏览时持续循环，悬停和点击不暂停；导航收起、文档隐藏时仍暂停不可见动画，编辑 / reduced motion 继续静态展示。

更新 `tests/header.spec.ts` 对应轮播检查，静态构建及导航 / 首屏定向检查 12 / 12 通过。本次未重跑完整回归或实体设备检查，未提交或推送。

## 会话交接与第二次 Git 检查点 · 2026-09-17

按用户要求新增 `doc/WEFT_PPL_Development_Handoff_2026-09-17.md`，区分明确要求与本项目观察到的偏好，汇总最终交互、误读及 bug 根因、代码入口、实际检查与未验证项。更新现有 `AGENTS.md` 增加必读入口和后续决定优先级，未创建重复 Agent.md；同步更正 README 中旧的导航闲置收起与暂停按钮说明。

本次提交前全量 51 项首次 49 项通过；2 项在写入 artifacts 截图时出现 UNKNOWN 文件错误，具体环境原因未确定。未改产品代码，使用 --last-failed 重跑 2 项全部通过，最终 51 项均已完成。当前源代码静态构建已通过，根路径静态导出检查的 103 次资源响应无失败，目标页刷新 / 返回与交互通过。三份入口文档的本地 Markdown 链接均存在。实验室样本：Edge 153 / Windows / i9-14900HX、1440×1000、回环网络、无 CPU 或网络限速，LCP 124ms、CLS 0.0131、脚本交互 INP 40ms、约 60fps；不代表真实用户数据。

推送目标仍为 Ace199/WEFT_PPL_Presentation 的 main；推送前已核对远程仍为上一检查点 0a1eb5e。本轮首页源码、测试、验证素材和交接文档共同提交。实际推送是否完成以远程提交核对为准；本文前面的“未推送”是各次修改当时的记录。网站未部署，真实生产素材、最终人工视觉确认和读者测试仍未完成。
